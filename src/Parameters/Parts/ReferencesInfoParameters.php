<?php

namespace Likemusic\BamboraCheckout\Parameters\Parts;

/**
 * @see https://dev.na.bambora.com/docs/references/checkout/#references-info
 */
interface ReferencesInfoParameters
{
    const REF1 = 'ref1'; // A custom identifier. 256 characters.

    const REF2 = 'ref2'; // A custom identifier. 256 characters.

    const REF3 = 'ref3'; // A custom identifier. 256 characters.

    const REF4 = 'ref4'; // A custom identifier. 256 characters.

    const REF5 = 'ref5'; // A custom identifier. 256 characters.
}
